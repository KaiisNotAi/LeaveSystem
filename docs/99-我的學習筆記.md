# 99 — 我的學習筆記

> 這份是個人學習筆記，會跟著專案一起推上 GitHub，可作為日後回顧或他人參考。
> 隨時可以新增、修改、整理思緒。

---

## 📅 學習日誌

### Day 1 — 模組 D 第 1~2 站（登入表單 → EF Core 查資料庫）

**做了什麼：**

- [x] 看完 `09-學習路徑.md` 模組 D 設計
- [x] 第 1 站：AccountController.Login (POST) — 看到模型繫結結果
- [x] 第 2 站：AuthService.ValidateAsync — 看到 EF Core 查詢結果

**觀察到的事：**

- `input.Username` = "Admin"、`input.Password` = "Admin@123"
  → ASP.NET Core 自動幫我把表單欄位填進 ViewModel
- `ModelState.IsValid` = true → `[Required]` 等屬性檢查都通過
- 即時運算視窗可以執行任何 C# 表達式（如 `input.Username == "Admin"`）
- `user` 撈出來時：
  - Id = 1、Username = "Admin"、DisplayName = "系統管理員"
  - PasswordHash 是一串 `$2a$11$...` 的 BCrypt 雜湊
  - UserRoles.Count = 1（被 Include 一起撈出來了）
- LINQ 集合不能用 `[0]`，要用 `.First()` → 因為 ICollection 不保證順序

**新學到的概念：**

- **Entity Framework Core**：把 C# 類別與資料表對應的 ORM 工具
- **DbContext**：資料庫進入點，每個 DbSet 是一張表
- **LINQ**：用 C# 寫查詢，EF Core 自動翻成 SQL
- **Include / ThenInclude**：Include 是主實體的關聯，ThenInclude 是關聯再關聯

---

### Day 2 — 模組 D 第 3~5 站（BCrypt → Claims → Cookie）

**做了什麼：**

- [x] 第 3 站：BCrypt 密碼比對（拆解雜湊字串、實驗 Verify）
- [x] 第 4 站：建立 ClaimsPrincipal（看 Claims 陣列一筆筆長出來）
- [x] 第 5 站：寫入 Cookie（觀察瀏覽器 F12 拿到的加密 Cookie）
- [x] 觀察「回程」：Cookie 帶回時 `User` 是怎麼還原的

**觀察到的事：**

- BCrypt 雜湊字串格式：`$2a$11$Salt(22字元)Hash(31字元)`
  - `$2a$` = 演算法版本
  - `$11$` = Work factor（2¹¹ 次迴圈，數字越大越慢越安全）
  - Salt 就明文寫在裡面 → 不是漏洞，是設計
- 同一個密碼 Hash 兩次結果**不同**（Salt 隨機），但 Verify 都會通過
- Verify 過程：從雜湊字串抽出 Salt → 用相同 Salt 重算 → 比對
- Claims 陣列：4 筆
  - `NameIdentifier` = "1"
  - `Name` = "系統管理員"
  - `Username` = "Admin"（自訂 Type）
  - `Role` = "Admin"
- **ClaimsIdentity 一定要指定 AuthenticationType**（"Cookies"），否則 `IsAuthenticated = false`
- 瀏覽器 F12 → Application → Cookies 看到的 `LeaveSystem.Auth` 是**加密亂碼**
- `document.cookie` 讀不到（因為 HttpOnly）→ 防 XSS 偷 Cookie
- 登入後任何 Controller 都能用 `User.Identity.Name`、`User.IsInRole("Admin")`、`User.FindFirst(ClaimTypes.NameIdentifier)?.Value`

**新學到的概念：**

- **Claim / ClaimsIdentity / ClaimsPrincipal 三層**：Claim=一行資訊、Identity=一組來自同來源的 Claims、Principal=完整使用者（皮夾裡可以有多張證件）
- **Data Protection API**：ASP.NET Core 內建加密，Cookie 內容用它加密簽章
- **UseAuthentication 必須在 UseAuthorization 之前**：先知道你是誰，才能判斷你能不能做
- **IsPersistent**：false = Session Cookie（關瀏覽器沒了）；true = 寫進硬碟到 ExpiresUtc 才過期

**卡關解決：**

- BCrypt Work Factor 12 vs 10 → 每 +1 時間翻倍，這就是「攻擊者破解成本翻倍」

---

### 🎉 模組 D 完成

```
模組 D：認證與授權
├── 第 1 站：AccountController.Login (POST)        ✅
├── 第 2 站：AuthService.ValidateAsync 查資料庫    ✅
├── 第 3 站：BCrypt 密碼比對                       ✅
├── 第 4 站：建立 ClaimsPrincipal                  ✅
└── 第 5 站：寫入 Cookie                           ✅
```

**掌握了：**
- MVC 請求生命週期
- EF Core 查詢 + Include 關聯載入
- BCrypt 密碼安全（Salt / Work Factor / Verify 原理）
- Claims 三層身分模型
- Cookie 認證的完整雙向流程（加密去、解密回）

---

### 下次要做什麼

⏭️ **進度標記：Phase 2 起點（班期 / 假別 / 簽核規則 CRUD）**

下次打開 VS 後對 AI 助手說：

> **「繼續 Phase 2」**

或

> **「繼續 Phase 2，目前所在進度：Phase 1 已完成、模組 D 學習完成，準備開始班期 / 假別 / 簽核規則 CRUD」**

Phase 2 三大交付物：
```
Phase 2：CRUD 管理後台
├── CohortController + Views     — 班期（建立、指派導師/科長/分署長）
├── LeaveTypeController + Views  — 假別（4 筆預設可增刪改）
└── ApprovalRuleController + Views — 簽核規則（3 條預設可調整門檻）
```

---

## 🗒️ 概念速查（自己整理）

### 一個 HTTP 請求的生命週期（簡化版）

```
[瀏覽器發 HTTP 請求]
		↓
Program.cs 中介軟體管線
  UseRouting        → 解析 URL 對應到哪個 Controller
  UseAuthentication → 從 Cookie 還原 ClaimsPrincipal
  UseAuthorization  → 檢查 [Authorize] 等規則
		↓
Controller Action 執行
		↓
回傳 View / JSON / Redirect
		↓
[瀏覽器顯示結果]
```

### 登入流程完整版

```
━━━━━━━━━━━━━━━━━━━━ 去程（登入這一次）━━━━━━━━━━━━━━━━━━━━

  瀏覽器                              伺服器
	│                                     │
	│  POST /Account/Login                │
	│  Username=Admin, Password=Admin@123 │
	│────────────────────────────────────▶│
	│                                     │ AccountController.Login
	│                                     │     ↓ (ModelState 驗證)
	│                                     │ AuthService.ValidateAsync
	│                                     │     ↓ (EF Core 查 DB + Include Roles)
	│                                     │ IPasswordHasher.Verify
	│                                     │     ↓ (BCrypt 比對)
	│                                     │ AuthService.SignInAsync
	│                                     │     ↓ (建 Claims/Identity/Principal)
	│                                     │ context.SignInAsync
	│                                     │     ↓ (Data Protection 加密序列化)
	│  HTTP 302 Redirect / Home           │
	│  Set-Cookie: LeaveSystem.Auth=xxx   │
	│◀────────────────────────────────────│
	│                                     │
	│ 存下 Cookie                          │
	│                                     │

━━━━━━━━━━━━━━━━━━━━ 回程（之後每次請求）━━━━━━━━━━━━━━━━━━━━

  瀏覽器                              伺服器
	│                                     │
	│  GET /Admin/Users                   │
	│  Cookie: LeaveSystem.Auth=xxx       │
	│────────────────────────────────────▶│
	│                                     │ UseAuthentication
	│                                     │     ↓ (解密 Cookie 還原 ClaimsPrincipal)
	│                                     │ HttpContext.User = principal
	│                                     │     ↓
	│                                     │ UseAuthorization
	│                                     │     ↓ (檢查 [Authorize])
	│                                     │ Controller Action
	│  HTTP 200 OK + HTML                 │
	│◀────────────────────────────────────│
```

### BCrypt 雜湊字串拆解

```
$2a$11$NmrQCP...(22字元)...abcdefg1234
 ↑   ↑     ↑                  ↑
 版  Work  Salt (隨機)        雜湊本體 (Salt+密碼算出來)
 本  factor                   
	 (2¹¹ 迴圈)
```

- **Salt 明文寫在雜湊裡** → 這不是漏洞，是設計
- Salt 目的是「讓相同密碼變成不同雜湊」，防彩虹表
- Verify 從雜湊字串抽 Salt → 用相同 Salt 重算 → 比對

### Claims 三層心智模型

```
🎫 ClaimsPrincipal (皮夾)
   ├── 📇 ClaimsIdentity #1 (身分證，AuthenticationType="Cookies")
   │    ├── Claim: NameIdentifier = "1"
   │    ├── Claim: Name = "系統管理員"
   │    ├── Claim: Username = "Admin"
   │    └── Claim: Role = "Admin"
   │
   └── 📱 ClaimsIdentity #2 (可能還有其他身分，我們現在只用一個)
```

| 概念 | 對應 |
|------|------|
| **Claim** | 一張證件上的**一行資訊** |
| **ClaimsIdentity** | 一張**證件**（同來源的一組 Claims）|
| **ClaimsPrincipal** | 一個**皮夾**（可能有多張證件）|

### Controller 常用取當前使用者資訊

```csharp
User.Identity.IsAuthenticated                          // 是否已登入
User.Identity.Name                                     // 顯示名稱
User.IsInRole("Admin")                                 // 是否為某角色
User.FindFirst(ClaimTypes.NameIdentifier)?.Value       // 使用者 ID (string)
```

### LINQ 常用方法

| 方法 | 一句話 |
|------|--------|
| `.First()` | 取第一個（空集合會炸）|
| `.FirstOrDefault()` | 取第一個（空集合回 null）|
| `.Single()` / `.SingleOrDefault()` | 取唯一一個（超過一個會炸）|
| `.Any()` | 是否有元素（比 Count > 0 快）|
| `.Where(...)` | 篩選 |
| `.Select(...)` | 投影/轉型 |
| `.Include(...)` | 連同關聯資料一起撈 |
| `.ToListAsync()` | 執行查詢並轉成 List |
| `.OrderBy(...)` / `.OrderByDescending(...)` | 排序 |

---

## 📚 我自己常用的快速鍵

| 快速鍵 | 用途 |
|--------|------|
| F5 | 啟動偵錯 / 繼續到下個中斷點 |
| Shift+F5 | 停止偵錯 |
| F9 | 設定/移除中斷點 |
| F10 | 逐步執行（不進入方法）|
| F11 | 逐步執行（進入方法）|
| Ctrl+Alt+I | 即時運算視窗 |
| Ctrl+Alt+B | 中斷點視窗 |
| Ctrl+Alt+L | 方案總管 |
| Ctrl+Shift+S | 儲存全部 |
| Ctrl+Shift+F4 | 關閉所有編輯器分頁 |

---

## 💭 學習心得區（隨意寫）

（在這裡寫您今天的感想、卡關的點、或想試的東西）
